name: Deploy Code

on: [push]

jobs:
  job1:
    environment: 
      name: testing
    env:
      # DATABASE_HOSTNAME: ${{secrets.DATABASE_HOSTNAME}}
      # DATABASE_PORT: ${{secrets.DATABASE_PORT}}
      # DATABASE_USERNAME: ${{secrets.DATABASE_USERNAME}}
      # DATABASE_PASSWORD: ${{secrets.DATABASE_PASSWORD}}
      # DATABASE_NAME: ${{secrets.DATABASE_NAME}}_tests
      # SECRET_KEY: ${{secrets.SECRET_KEY}}
      # ALGORITHM: ${{secrets.ALGORITHM}}
      DATABASE_HOSTNAME: localhost
      DATABASE_PORT: 5432
      DATABASE_USERNAME: postgres
      DATABASE_PASSWORD: superuser
      DATABASE_NAME: fastapi_test
      SECRET_KEY: 09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 30

    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: superuser # ${{secrets.DATABASE_PASSWORD}}
          POSTGRES_DB: fastapi_test # ${{secrets.DATABASE_NAME}}_tests
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        
    steps:
      - name: pulling git repo
        uses: actions/checkout@v3
      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9' 
      - name: update pip
        run: python -m pip install --upgrade pip
      - name: install dependencies
        run: pip install -r requirements.txt
      - name: test with pytest
        run: |
          pip install pytest
          pytest tests/test_root.py -v -s